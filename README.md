# WoW Guide Manager

Portable Windows desktop MVP for a World of Warcraft guide platform. The first content pack focuses on WoW Classic; the data model is prepared for later expansions and subscription-gated premium content.

## Product goals

- Become a complete A-to-Z hub for WoW guides: leveling, dungeons, raids, professions, gold, PvP, classes, reputation, addons, secrets and rare spawns.
- Start with World of Warcraft Classic and add future expansions as modular content packs.
- Separate free and premium content so a subscription model can be integrated without changing the guide UI.
- Ship as a portable Windows-ready package without external runtime dependencies beyond a modern browser.

## Tech stack

- Vanilla HTML, CSS and JavaScript modules
- Node.js build and test scripts
- No third-party NPM packages required for the current MVP
- Portable Windows launcher script generated by the build

## Getting started

```bash
npm install
npm run build
```

Open `dist/index.html` in a modern browser.

## Build commands

```bash
npm run build
npm run dist:win
```

`npm run build` creates the app bundle in `dist/`. `npm run dist:win` additionally creates `release/WoW-Guide-Manager-0.1.0-portable/` with a Windows `.cmd` launcher and all app files.

## Content model

Guide content currently lives in `src/data/guides.js`. Each guide has an expansion key, category, audience level, estimated reading time, checklist items, premium flag and tags. New expansions can be added by extending the `expansions` roadmap array and guide data.

## Subscription roadmap

The app already presents three tiers: Scout, Hero and Legend. Production monetization should add:

1. Payment provider integration, such as Stripe or Paddle.
2. License server for entitlement checks.
3. Signed content-pack manifests.
4. Offline grace periods for portable users.
5. Account management and GDPR-compliant data export/deletion.
